[project]
name = "energy-system"
version = "0.1.0"
requires-python = ">=3.10, <3.13"
dependencies = [
    "flowcean",
    "midas-palaestrai>=3.5.6.post1",
]

[tool.pyright]
extends = "../../pyproject.toml"

[tool.uv.sources]
flowcean = { workspace = true }
