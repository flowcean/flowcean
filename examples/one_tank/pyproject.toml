[project]
name = "one-tank"
version = "0.1.0"
requires-python = ">=3.12, <4.0"
dependencies = ["flowcean[river,torch]"]

[tool.pyright]
extends = "../../pyproject.toml"

[tool.uv.sources]
flowcean = { workspace = true }
