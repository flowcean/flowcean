[project]
name = "linear-data"
version = "0.1.0"
requires-python = ">=3.12, <4.0"
dependencies = ["flowcean[torch]"]

[tool.pyright]
extends = "../../pyproject.toml"

[tool.uv.sources]
flowcean = { workspace = true }
