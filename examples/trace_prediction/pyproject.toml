[project]
name = "flowcean-example-trace-prediction"
version = "0.1.0"
requires-python = ">=3.12, <4.0"
dependencies = ["flowcean"]

[tool.uv.sources]
flowcean = { workspace = true }

[tool.pyright]
extends = "../../pyproject.toml"
