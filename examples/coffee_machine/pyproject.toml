[project]
name = "coffee-machine"
version = "0.1.0"
requires-python = ">=3.12, <4.0"
dependencies = ["flowcean", "polars>=1.33.0", "tqdm>=4.66.5"]

[tool.pyright]
extends = "../../pyproject.toml"

[tool.uv.sources]
flowcean = { workspace = true }
