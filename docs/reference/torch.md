# PyTorch

`flowcean.torch` provides datasets, model wrappers, neural network architectures, and a Lightning learner.
Install the `torch` extra as described in [installation](../getting_started/installation.md).

::: flowcean.torch
